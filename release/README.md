# release

这里用于存放发布物：

- `GameDesigner.exe`
- `GameDesigner-Setup.exe`

`GameDesigner-Setup.exe` 会把主程序、Node.js 运行时、Codex CLI、Claude Code CLI 一起装到本机用户目录。

从项目根目录执行：

```powershell
.\build_release.bat
```
